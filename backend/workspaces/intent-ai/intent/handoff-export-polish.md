# Handoff export polish

The handoff export must include a per-department summary line at the top.
Exported markdown must always render obligation ids as footnotes, never inline.
The export must be reachable from the dashboard with one click.